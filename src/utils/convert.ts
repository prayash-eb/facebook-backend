export const floor = (value: number): number => {
    return Math.floor(value)
}
export const ceil = (value: number): number => {
    return Math.ceil(value)
}
export const max = (agr1: number, arg2: number): number => {
    return Math.max(agr1, arg2)
}